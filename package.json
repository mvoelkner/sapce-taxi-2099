{
  "name": "space-taxi-2099",
  "version": "1.0.0",
  "private": true,
  "description": "Space Taxi 2099 — the native package. The web game itself needs none of this and is still a plain index.html.",
  "scripts": {
    "icons": "node scripts/make-icons.js",
    "levels": "node scripts/extract-levels.js",
    "www": "node scripts/build-www.js",
    "test": "node test/harness.js",
    "test:wire": "node test/wire.mjs",
    "test:online": "node test/online.mjs",
    "sync": "npm run www && npx cap sync",
    "ios": "npm run sync && npx cap open ios",
    "android": "npm run sync && npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^6.2.0",
    "@capacitor/haptics": "^6.0.2",
    "@capacitor/ios": "^6.2.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
